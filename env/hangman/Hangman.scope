
func int sum(array<int> nums) {
    int total = 0;
    for (int i = 0; i < nums.length; i++) {
        total += nums[i];
    }
    ret total;
}
func void main() {
   array<int> nums = [1, 2, 3, 4];
   int test=sum(nums);
   print(test);
}
